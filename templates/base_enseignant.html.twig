{% extends 'base.html.twig' %}

{% block body_class %}dashboard-body{% endblock %}

{% block body %}
<div class="dashboard-wrapper">

    <!-- ===== SIDEBAR ENSEIGNANT ===== -->
    <aside class="dashboard-sidebar" id="enseignantSidebar">
        <div class="sidebar-logo">
            <a href="{{ path('enseignant_dashboard') }}">
                <img src="{{ asset('logo/logo_horizontal.png') }}" alt="SoutenancePro">
            </a>
            <div class="app-title">Espace Enseignant</div>
        </div>

        <nav class="sidebar-nav">
            <div class="sidebar-section-title">Menu principal</div>
            <ul>
                <li>
                    <a href="{{ path('enseignant_dashboard') }}"
                       class="{{ app.request.attributes.get('_route') == 'enseignant_dashboard' ? 'active' : '' }}">
                        <span class="nav-icon flaticon-home"></span>
                        <span class="nav-text">Tableau de bord</span>
                    </a>
                </li>
                <li>
                    <a href="{{ path('enseignant_soutenances') }}"
                       class="{{ app.request.attributes.get('_route') == 'enseignant_soutenances' ? 'active' : '' }}">
                        <span class="nav-icon flaticon-book"></span>
                        <span class="nav-text">Mes soutenances</span>
                    </a>
                </li>
                <li>
                    <a href="{{ path('enseignant_jurys') }}"
                       class="{{ app.request.attributes.get('_route') == 'enseignant_jurys' ? 'active' : '' }}">
                        <span class="nav-icon flaticon-test"></span>
                        <span class="nav-text">Mes jurys</span>
                    </a>
                </li>
            </ul>

            <div class="sidebar-section-title" style="margin-top: 20px;">Compte</div>
            <ul>
                <li class="nav-logout">
                    <a href="{{ path('app_logout') }}">
                        <span class="nav-icon fa fa-sign-out"></span>
                        <span class="nav-text">Déconnexion</span>
                    </a>
                </li>
            </ul>
        </nav>
    </aside>

    <!-- ===== MAIN AREA ===== -->
    <div class="dashboard-main">

        <!-- Top Header -->
        <header class="dashboard-header">
            <div class="d-flex align-items-center gap-3">
                <button class="sidebar-toggle" onclick="toggleSidebar()" title="Menu">
                    <i class="fa fa-bars"></i>
                </button>
                <h1 class="page-title mb-0">{% block page_title %}Espace Enseignant{% endblock %}</h1>
            </div>

            <div class="header-right">
                <div class="user-dropdown dropdown">
                    <button class="btn dropdown-toggle" type="button" data-toggle="dropdown">
                        <div class="user-avatar">
                            {{ app.user ? app.user.email|first|upper : 'E' }}
                        </div>
                        <span>
                            {% if app.user and app.user.enseignant %}
                                {{ app.user.enseignant.prenom }} {{ app.user.enseignant.nom }}
                            {% else %}
                                {{ app.user ? app.user.email : 'Enseignant' }}
                            {% endif %}
                        </span>
                    </button>
                    <div class="dropdown-menu dropdown-menu-right">
                        <a class="dropdown-item text-danger" href="{{ path('app_logout') }}">
                            <i class="fa fa-sign-out mr-2"></i> Déconnexion
                        </a>
                    </div>
                </div>
            </div>
        </header>

        <!-- Flash Messages -->
        <div class="dashboard-content" style="padding-bottom: 0; padding-top: 15px;">
            {% for type, messages in app.flashes %}
                {% for message in messages %}
                    <div class="flash-{{ type == 'success' ? 'success' : 'error' }}">
                        <i class="fa fa-{{ type == 'success' ? 'check-circle' : 'exclamation-circle' }} mr-2"></i>
                        {{ message }}
                    </div>
                {% endfor %}
            {% endfor %}
        </div>

        <!-- Page Content -->
        <main class="dashboard-content">
            {% block content %}{% endblock %}
        </main>

        <!-- Footer -->
        <footer style="text-align:center; padding: 15px; font-size: 12px; color: #aaa; border-top: 1px solid #f0f0f8; background: #fff;">
            &copy; {{ "now"|date("Y") }} SoutenancePro &mdash; Gestion des soutenances
        </footer>
    </div>
</div>

{% block javascripts %}
{{ parent() }}
<script>
function toggleSidebar() {
    document.getElementById('enseignantSidebar').classList.toggle('open');
}
</script>
{% endblock %}
{% endblock %}
